# =============================================================================
# Configuration Pepper Parapharmacie - Mode Choregraphe
# =============================================================================
# Copiez ce fichier vers .env et remplissez vos valeurs :
#   cp .env.example .env

# -----------------------------------------------------------------------------
# OpenAI API (obligatoire pour la réponse LLM)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-votre-cle-api-ici

# -----------------------------------------------------------------------------
# Robot Pepper
# -----------------------------------------------------------------------------
# IP du robot Pepper (pour TTS via qi si disponible)
PEPPER_IP=192.168.x.x
PEPPER_PORT=9559

# -----------------------------------------------------------------------------
# Serveur speech_server.py (Flask)
# -----------------------------------------------------------------------------
# Port sur lequel tourne speech_server.py (à configurer aussi dans SpeechRecognitionBox.py)
FLASK_PORT=5001

# Chemin vers la base de données produits
DB_PATH=data/products.db
BLACKLIST_PATH=data/blacklist.json

# -----------------------------------------------------------------------------
# Serveur Tablette WebSocket
# -----------------------------------------------------------------------------
TABLET_PORT=8765

# -----------------------------------------------------------------------------
# Vision / VLM (optionnel - Apple Silicon uniquement)
# -----------------------------------------------------------------------------
# Désactiver le VLM si non disponible
PEPPER_VLM_DISABLED=0

# Modèle VLM local (MLX sur Apple Silicon)
VLM_MODEL=mlx-community/Qwen2-VL-2B-Instruct-4bit

# -----------------------------------------------------------------------------
# Choregraphe - Ports TCP (BridgeSupervisorBox.py)
# -----------------------------------------------------------------------------
PEPPER_CHOREGRAPHE_AUDIO_CAPTURE_PORT=5555
PEPPER_CHOREGRAPHE_AUDIO_PLAYBACK_PORT=5556
PEPPER_CHOREGRAPHE_VIDEO_PORT=5557
PEPPER_CHOREGRAPHE_CONTROL_PORT=5558
PEPPER_CHOREGRAPHE_AUDIO_SAMPLE_RATE=16000
PEPPER_CHOREGRAPHE_AUDIO_CHANNELS=1

# -----------------------------------------------------------------------------
# Logs
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
